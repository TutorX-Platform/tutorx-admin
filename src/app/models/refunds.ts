export interface Refund {
  id: string;
  studentId: string;
  tutorId: string;
  title: string;
  message: string;
  questionId: string;
  chatId: string;
  refundStatus: string;
  refundAmount: string;
}
